@{
    ViewBag.Title = "Home";
}
@section featured {    
    <section class="featured">
        <div class="content-wrapper">
            <hgroup class="title">                
            </hgroup>
        </div>
    </section>
}
<h3>Active Features</h3>
<ul class="round">
        @foreach (var feature in ViewBag.Features.Keys)
        {
            <li><span class="inlinesparkline">@ViewBag.Features[feature]</span>@feature</li>
        }
</ul>


@section scripts {
    <script type="text/javascript">
        $("ul.nav li.dashboard").addClass("active");
        $('.inlinesparkline').sparkline();
    </script>
}